{
  "name": "cypress-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --headed --browser chrome",
    "cy:headless": "cypress run",
    "cy:parallel": "cypress-parallel -s cy:run -t3 -d 'cypress/e2e/parallelForEach' "
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.5",
    "cypress": "^14.2.0",
    "cypress-file-upload": "^5.0.8",
    "cypress-iframe": "^1.0.1",
    "cypress-parallel": "^0.15.0"
  }
}
